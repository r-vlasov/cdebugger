#include <signal.h>
#include <stdio.h>
void handler(int signo)
{
	fprintf(stderr, "hello");
	while(1);
}


int main()
{
	signal(SIGSEGV, handler);
	const char* s = "hello";
	*(char*) s = 'H';
	return 0;
}
